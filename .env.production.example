# 🚀 Variáveis de Ambiente para Produção (Railway)
# Copie este arquivo e configure os valores reais nos GitHub Secrets

# ============================================
# 🔐 Railway Credentials
# ============================================
RAILWAY_TOKEN=RAILWAY_TOKEN_xxx_xxx_xxx
RAILWAY_PROJECT_ID_API=abc123-def456-ghi789
RAILWAY_PROJECT_ID_WEB=xyz987-uvw654-rst321

# ============================================
# 🗄️ Database (PostgreSQL no Railway)
# ============================================
# Obtenha esta URL no Railway Dashboard → PostgreSQL service → Variables
# IMPORTANTE: Adicione ?schema=public no final
DATABASE_URL=postgresql://postgres:senha@host.railway.internal:5432/railway?schema=public

# ============================================
# 🔑 Authentication
# ============================================
# Gere um secret forte (32+ caracteres)
# PowerShell: [Convert]::ToBase64String((1..32 | ForEach-Object { Get-Random -Minimum 0 -Maximum 256 }))
JWT_SECRET=your-super-secret-jwt-key-min-16-chars

# ============================================
# 🌐 API Configuration
# ============================================
# URL pública da API no Railway (obtenha após primeiro deploy)
# Formato: https://[seu-projeto]-api.up.railway.app
VITE_API_URL=https://limpacelular-api.up.railway.app

# ============================================
# 📝 Notas Importantes
# ============================================
# 1. Nunca commite este arquivo com valores reais
# 2. Configure estes valores como Secrets no GitHub:
#    - Settings → Secrets and variables → Actions → New repository secret
# 3. A URL da API só estará disponível após o primeiro deploy
# 4. Para obter URLs das aplicações no Railway:
#    - Vá no projeto → Service → Settings → Networking → Generate Domain
